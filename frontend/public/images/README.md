# Landing background image

Place the final landing-page background image in this folder using this exact filename:

```text
landing-background.jpg
```

Recommended size: at least `1920 x 1080`, landscape orientation. The application already
references `/images/landing-background.jpg`; when the file is absent, the built-in blue
gradient placeholder is displayed automatically.
